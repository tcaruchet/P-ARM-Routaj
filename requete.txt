ldr      R0#150
ldr R1#250
ldr R2#-500
ldr R3#250
ldr R4#0
ldr R7#7
ldr R8#0
ldr R9#550
ldr R10#3
if: BEQ R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo



if: BNE    R2,R3



else:
if: BCS R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BCC R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BMI R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BPL R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BVS R9,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BVC R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BHI R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BLS R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BGE R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BLT R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BGT R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo
if: BLE R0,R1
	yolo
	yolo
	yolo
else: 
	pas yolo
	pas yolo

if: LSLS R7,R8#3
	yolo
	yolo

else:
	pas yolo
if: LSRS R7,R8#3
	yolo
	yolo

else:
	pas yolo
if: ASRS R7,R8#3
	yolo
	yolo

else:
	pas yolo